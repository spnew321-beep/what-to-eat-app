# 補充定案：食物類型擴充（自訂類型）

> 這份文件是針對「這餐吃什麼」App 食物類型功能的擴充討論定案結果。**這份文件已經是討論完的定案結果，不是待確認的草案**，Claude Code 可以直接照著做，不需要再回頭跟使用者確認這些規則。
>
> 搭配 `CLAUDE_CODE_專案交接文件.md`（第 5 節 schema）與 `補充-UIUX優化定案.md` 一起閱讀。本文件的 schema 異動要合併進第 5 節的 SQL 一起建表，不要單獨拆開執行。

---

## 背景

目前的食物類型（`CATEGORIES`：中式/日式/韓式/西式/東南亞/小吃/早餐/飲料甜點）是寫死在前端程式碼裡的固定陣列，使用者無法新增自己需要的類型。這次定案讓類型可以擴充。

## 定案內容

### 1. 預設類型與自訂類型並存
- 保留現有 8 種內建預設類型，不能刪除、不能改名
- 使用者可以另外新增自己的「自訂類型」，兩者並存使用

### 2. 新增入口
- 在新增/編輯餐廳表單裡，選類型的介面直接提供「＋新增自訂類型」選項，不另外做獨立的類型管理畫面

### 3. 自訂類型的篩選與建議邏輯
- 自訂類型跟預設類型一樣，可以當篩選條件使用（收藏頁篩選、系統建議模式的類型篩選都要能用）
- 收藏頁篩選列中，自訂類型併入現有「更多類型」收合區塊，不另外分一排新區塊

### 4. 刪除與改名
- 自訂類型支援刪除與重新命名
- **改名**：連動更新所有已使用這個類型的餐廳，把它們的 `category` 一併改成新名稱
- **刪除**：允許直接刪除，即使目前有餐廳正在使用這個類型；刪除後，原本用這個類型的餐廳自動改為「未分類」

### 5. Schema 需求

預設類型維持在前端寫死，不進資料庫；自訂類型需要一張新表管理：

```sql
create table custom_categories (
  id uuid primary key default gen_random_uuid(),
  owner_user_id uuid references auth.users(id) on delete cascade not null,
  name text not null,
  created_at timestamptz default now(),
  unique (owner_user_id, name)
);
```

`restaurants.category` 欄位維持既有的 `text` 型別（本來就不是外鍵約束），改名/刪除自訂類型時，由應用層邏輯連動更新 `restaurants` 表中相符的 `category` 文字值（比照 `decision_history.restaurant_name` 那樣，用文字比對而非外鍵關聯，不需要額外遷移既有資料結構）。

啟用 Row Level Security，套用與其餘表一致的 `owner_user_id = auth.uid()` 規則。

---

## 給 Claude Code 的補充指示

以上是討論後的定案結果，不需要再回頭跟使用者確認規則本身。`custom_categories` 表請合併進 `CLAUDE_CODE_專案交接文件.md` 第 5 節的建表流程一起執行。實作改名/刪除自訂類型的功能時，記得同步處理 `restaurants.category` 欄位的連動更新（改名 → 批次更新為新名稱；刪除 → 批次更新為「未分類」）。
