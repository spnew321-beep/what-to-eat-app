-- ============================================================================
-- 這餐吃什麼 App — restaurants.x / restaurants.y (v5 補充)
-- ============================================================================
-- 交接文件第 5 節原本說「原型的相對座標(x/y%)不搬過來,一開始留空」,
-- 只規劃了 lat/lng(真經緯度,留給以後真的接 Google 地圖)。但原型目前
-- 唯一能用的地圖功能(🗺️ 地圖分頁點格線板新增店家)就是靠這組相對座標
-- 定位大頭針,拿掉的話等於這個功能接上真資料庫後直接壞掉。
--
-- 這裡務實地把 x/y 也加進來,跟 lat/lng 並存:x/y 是目前這版「假地圖
-- 格線板」在用的 0-100 相對座標,lat/lng 繼續留給以後真的串 Google 地圖
-- 時使用,兩者不是同一件事,不要合併。
-- ============================================================================

alter table restaurants add column x double precision;
alter table restaurants add column y double precision;
