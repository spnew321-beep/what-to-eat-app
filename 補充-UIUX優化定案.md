# 補充定案：UI/UX 優化與缺漏功能補完

> 這份文件是針對「這餐吃什麼」App 進行一次完整 UI/UX 檢視後的討論定案結果，涵蓋畫面重構、缺漏功能（編輯）、新功能（菜單照片、自訂標籤）與幾項一致性修正。**這份文件已經是討論完的定案結果，不是待確認的草案**，Claude Code 可以直接照著做，不需要再回頭跟使用者確認這些規則。
>
> 搭配 `CLAUDE_CODE_專案交接文件.md`（第 5 節 schema、第 7 節視覺語言）與 `補充-meal_events共用框架.md` 一起閱讀。本文件的 schema 異動要合併進第 5 節的 SQL 一起建表，不要單獨拆開執行。

---

## 1. 協作場次「進行中」畫面重構

**現況問題**：候選店家、表態、參與者三塊內容全部無分段地垂直疊在同一頁，手機上要滑很長；先表態模式下候選店家還會被列兩次（一次是已選 pills，一次是表態列表），格式不同、資訊重複。

**定案**：
- 改用**頁內 Tab 分頁**，切換「候選 / 表態」「參與者」等子頁籤，不再垂直全部塞在一起
- 表態動作（✅❌）**直接整合進候選店家列表本身**，每列右側放✅❌即可，不再另外重複列一次表態專用清單
- **移除**「模擬新增訪客」「立即結算（測試用）」這兩顆原型 demo 按鈕——正式版靠真實邀請連結 + 第 6 節的伺服器排程雙保險結算機制，不需要這兩顆手動觸發的按鈕

---

## 2. 編輯餐廳功能（缺漏功能補完）

**現況問題**：目前完全沒有編輯已存在餐廳的功能，收藏列表與地圖圖釘彈窗都只有「收藏★」「刪除🗑」，想改任何欄位只能刪除重新新增。這是功能缺漏，不是體驗優化，優先度最高。

**定案**：
- **入口**：收藏列表每列加一個「編輯」圖示；地圖圖釘彈窗也加「編輯」按鈕；兩者共用同一個表單元件（跟新增表單同一份，差別只在預填現有資料 vs. 空白）
- **涵蓋欄位**：現有欄位（店名/類型/價位/人物標籤/備註）之外，這次一併加入兩個新欄位：

### 2.1 菜單照片
- 每間餐廳可上傳**多張**菜單照片，UI 需要小張圖中圖圖示（縮圖網格或輪播）
- 儲存方式**交給 Claude Code 依專案慣例決定**（Postgres 已用 Supabase，若無特殊理由建議直接用 Supabase Storage 以簡化整合，但不強制）
- Schema 需求：新增一張 `restaurant_photos` 表存多張照片路徑，不要把照片路徑塞進 `restaurants` 表的單一欄位

```sql
create table restaurant_photos (
  id uuid primary key default gen_random_uuid(),
  restaurant_id uuid references restaurants(id) on delete cascade not null,
  storage_path text not null,
  created_at timestamptz default now()
);
```

### 2.2 自訂標籤
- **自由文字標籤**，例如「適合約會」「平日券」，與現有的食物類型、人物標籤（tagIds）完全無關，純粹是帳號自己的分類方式
- 收藏頁篩選加**第三排篩選「自訂標籤」**，因為標籤數量可能較多、內容不固定，**用下拉選單呈現，不要用 chip 橫排**（避免版面被撐爆，這點跟「食物類型」「成員喜愛的店」兩排 chip 的呈現方式刻意不同）
- Schema 需求：

```sql
alter table restaurants add column custom_tags text[] default '{}';
```

---

## 3. 空按鈕提示文字客製化

**現況問題**：目前所有 placeholder 按鈕（複製邀請連結、隱私權政策、服務條款、帳號設定、意見回饋等）都共用同一句 `notReady()` toast：「此功能尚未開放，之後接上資料庫才會實裝」，使用者不知道按下去原本該發生什麼。

**定案**：**改為逐項客製化文字**，每個按鈕依照它實際缺的東西各寫一句話，不要共用同一句。範例格式：

- 複製邀請連結 → 「連結功能需要帳號系統才能使用」
- 隱私權政策 / 服務條款 → 依實際缺的內容各自說明（例如「需接上正式條款內容」）
- 帳號設定 → 「需要帳號系統才能使用」
- 意見回饋 → 依實際情況說明

Claude Code 建立這些按鈕時，請逐一撰寫貼合情境的句子，不要沿用原型的統一文案。

---

## 4. 一致性修正（四項，全部要做）

| # | 問題 | 定案 |
|---|---|---|
| 1 | 刪除餐廳目前沒有確認，跟刪除標籤/群組（有 confirm 對話框）不一致 | 刪除餐廳也加確認彈窗，與刪除標籤/群組的保護力道一致 |
| 2 | 協作場次設定截止時間時，preset chip（1小時後/今晚8點/明天中午/本週末）點下去沒有任何選中樣式變化 | 選中後加 active 樣式，不要只靠下方小字提示確認有沒有點成功 |
| 3 | 群組內「加入既有標籤」的展開/收合狀態用 inline style 控制，每次觸發其他 render 就會被重置回收合狀態 | 改成存進 state，不因畫面重新渲染而被重置 |
| 4 | 地圖圖釘彈窗用 `transform: translate(-50%, -100%)` 定位，點在畫面偏上方時彈窗可能被裁切看不到內容 | 彈窗加邊界偵測，自動調整位置避免被裁切出畫面外 |

---

## 給 Claude Code 的補充指示

以上四節都是討論後的定案結果，不需要再回頭跟使用者確認規則本身。開始動手前建議的順序：

1. 先做第 2 節的編輯功能（含 schema 異動），這是功能缺漏，優先度最高
2. 再做第 1 節的協作場次畫面重構
3. 第 3、4 節屬於小範圍修正，可以合併在同一輪一起處理

第 2 節的兩個新 schema（`restaurant_photos` 表、`restaurants.custom_tags` 欄位）請合併進 `CLAUDE_CODE_專案交接文件.md` 第 5 節的建表流程一起執行，套用同一套 `owner_user_id = auth.uid()` 的 Row Level Security 規則（`restaurant_photos` 透過 `restaurant_id` 關聯回 `restaurants` 的擁有者判斷權限）。
